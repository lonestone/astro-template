---
import logo from '../assets/icons/logo.svg'
import config from '../../website.config'

interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<img src={logo.src} alt={config.siteName} class={className} />
